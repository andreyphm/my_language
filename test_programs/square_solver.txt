возьмём <my_stdlib>

докажем теорема()
■
    пусть a определим_как дано() §
    пусть b определим_как дано() §
    пусть c определим_как дано() §

    решение_уравнения(a, b, c) §
    сводится_к_предыдущему §
□

докажем решение_уравнения(a, b, c)
■
    предположим (a равняется 0)
    ■
        решение_линейного_уравнения(b, c) §
    □
    в_противном_случае
    ■
        решение_квадратного_уравнения(a, b, c) §
    □

    сводится_к_предыдущему §
□

докажем решение_линейного_уравнения(b, c)
■
    предположим (b равняется 0)
    ■
        предположим (c равняется 0)
        ■
            найдётся(0 без 1) §
        □
        в_противном_случае
        ■
            найдётся(0) §
        □
    □
    в_противном_случае
    ■
        найдётся(1) §
        найдётся(((0 без 1) умножим_на c) поделим_на b) §
    □
□

докажем решение_квадратного_уравнения(a, b, c)
■
    пусть D определим_как b умножим_на b без 4 умножим_на a умножим_на c §

    предположим (близко_к_нулю(D))
    ■
        пусть x1 определим_как ((0 без 1) умножим_на b) поделим_на (2 умножим_на a) §
        найдётся(1) §
        найдётся(x1) §
        сводится_к_предыдущему §
    □

    предположим (D > 0)
    ■
        пусть x1 определим_как (((0 без 1) умножим_на b вместе_с корень(D)) поделим_на (2 умножим_на a)) §
        пусть x2 определим_как (((0 без 1) умножим_на b без корень(D)) поделим_на (2 умножим_на a)) §
        найдётся(2) §
        найдётся(x1) §
        найдётся(x2) §
        сводится_к_предыдущему §
    □
    в_противном_случае
    ■
        найдётся(0) §
    □

    сводится_к_предыдущему §
□

докажем модуль(значение)
■
    предположим (значение < 0)
    ■
        сводится_к_предыдущему 0 без значение §
    □

    сводится_к_предыдущему значение §
□

докажем близко_к_нулю(значение)
■
    пусть ε определим_как 0.000001 §

    предположим (модуль(значение) < ε)
    ■
        сводится_к_предыдущему 1 §
    □

    сводится_к_предыдущему 0 §
□
